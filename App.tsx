// Root entry for Expo when building from the monorepo root.
// Defer to the mobile workspace App entry so module resolution follows that package.
import App from './apps/mobile/App';

export default App;
